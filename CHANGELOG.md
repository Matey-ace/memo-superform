# 更新日志

## Unreleased — 语音包快速挂载与待补齐提示

- 设置中的 GPT-SoVITS 区域新增“快速挂载语音包”：将完整或待补齐的 `tts_pack` ZIP 拖入即可安装到 EXE 同级的本地数据目录。
- 只要 ZIP 含有效 `pack.json` 就允许挂载；若缺少 `.venv311`、`tts_engine`、GPT / SoVITS 模型、参考音频、参考文本、语言或 Live2D 绑定，设置页会逐项显示缺失内容和预期位置，方便后续自行补齐。
- 挂载采用流式落盘、ZIP 路径/链接/解压体积校验和临时目录原子切换；缺少清单、清单损坏或 ZIP 不安全时不会覆盖正在使用的语音包。
- 挂载前会关闭空闲的旧 TTS worker，挂载后默认关闭语音开关，避免旧模型缓存或旧角色状态继续被复用；用户确认 Live2D 绑定后再启用语音。
- 修复同名 Live2D 模型重新导入/下载时先删除旧目录的问题：候选模型会先完整校验，再原子替换；文件或数据库提交失败都会恢复旧模型和注册信息。
- 汉化自有 Python、JavaScript 与样式代码中的大部分英文说明注释，保留 API、类名和协议名等必要技术标识。
- 补充 Bestdori Live2D 下载流程与 Cherry Studio Codex 集成的参考来源，以及渲染器、桌面运行时和本地字体的第三方声明及许可证；中英文 README 分别列出实际参考范围和 Memo 自行实现边界。

## 0.77 — 角色资料包单入口与运行保护

- 移除容易混淆的全局“模型文件上传”框。GPT、SoVITS、参考音频和可选索引现在只在“编辑选中角色”中管理，上传目标、已关联文件和启用角色会明确显示。
- 角色资源严格固定为各自目录中的规范文件，并由 manifest 精确解析；旧上传接口已退役，任何请求都不能再把文件写入旧音色目录或用排序猜测模型/音频。
- 选中角色与已启用角色彻底分离：编辑草稿不会影响当前陪伴音色；切换或更新启用角色后会重置 TTS worker，并只允许当前完整角色参与预加载和合成。
- 修复“显示已启用但实际没有声音”的状态漏洞：缺失物理资源时会明确标记角色未就绪，拒绝启用/预加载/合成，补齐资源后才恢复可用。
- Live2D 切换前会刷新模型库，并为 WebGL、Pixi 插件、模型 URL、画布尺寸和加载失败提供可复制的诊断信息；正在被角色绑定的 Live2D 模型不可删除。
- 角色资料、Live2D 绑定与启用状态改为串行事务：保存或切换中任一环节失败会回滚，避免语音角色与显示模型分别停留在不同角色。
- 角色人设也随资料包保存和切换；即使多个角色共用同一个 Live2D 模型，仍会使用各自的人设与语气。
- 连续触摸朗读改为单飞并保留最新一句，不再中止服务器仍在生成的请求；生成器会完整消费并播放当前回应，避免触摸后长期显示忙碌却没有声音。
- 首次语音合成会给模型加载保留更长的等待窗口，后续合成仍保持短超时保护；浏览器也不会在冷启动 45 秒时提前取消请求。
- 语音启动前会实际检查推理依赖。缺少日文 `pyopenjtalk` 等模块时设置页会显示“修复语音环境”，并使用 Windows 预编译运行时修复；修复成功后会恢复此前的语音开关。
- v0.77 使用独立实例通道启动，避免双击新版本时被正在运行的 v0.76 唤醒并静默退出。
- 为控制 Windows EXE 体积，v0.77 发布包未随附旧 SQL Server 只读迁移所需的可选 `pyodbc` 组件；常规 SQLite 数据、Live2D 和外置 GPT-SoVITS 语音资源包不受影响。

## 0.76 — 统一角色包与参考音频修复

- 新增角色资料包：每个角色独立绑定 GPT/SoVITS 模型、参考音频、逐字参考文本、参考语言和 Live2D 模型；资料未配齐时不可启用。
- 首次使用自动保留并迁移旧资料：原始祥子模型与日文参考资料迁为“丰川祥子”，旧上传的 `gpt.ckpt` / `sovits.pth` 隔离为“千早爱音”草稿，杜绝模型与参考音频混用。
- 参考资源改为 manifest 精确指定，不再按目录名排序猜测“第一个”模型或音频；切换或更新角色资料后自动重置 TTS worker。
- 修复带说明文字的旧参考语言文件无法解析数字 `3` 的问题，日文参考资料会正确按日文处理。

## 0.75 — D_sakiko 语音/音色/生成时间调参

### 语音生成、音色调整（参考 D_sakiko）

- 设置面板新增「句间停顿」「采样度数 top_k」「文本切分方式」「随机种子」，可实时调整生成语音的节奏、自然度与稳定性。
- 后端 `/api/tts/speak` 与 `TTSManager.synthesize` 透传 `fragment_interval`、`top_k`、`text_split_method`、`seed`，默认值与原有行为保持一致。
- 参数值保存于本地 `localStorage`，下次启动继续沿用，不影响未开启用户的原有音色。

### 生成时间 / 性能

- 新增「GPU 加速（CUDA graph）」与「并行推理」开关，透传 `use_cuda_graph`、`parallel_infer`，供 N 卡用户加速长句生成。
- 纯 CPU 或旧显卡建议保持默认关闭，避免推理异常。

### 语音模型导入

- 新增 GPT-SoVITS 模型文件拖拽导入（`*.ckpt` / `*.pth` / 可选 `*.index`），自动归位到当前音色目录。

### 触摸互动简化

- 触摸角色时不再先用本地台词秒回，改为直接让 AI 生成并朗读唯一回复；仅在未配置 AI 或调用失败时回退为本地台词，避免一次触发出现两套回应。

### 语音修复

- 修复陪伴语音常只读出单个语气词（如“嗯”“啊”）的问题：收紧 AI 即时回复提示，要求 8~36 个汉字、自然成句，并为 AI 回复加了长度兜底（过短时改用完整的本地台词），确保朗读的是完整内容。

## 0.74 — 角色人设与目录检索修复

### 角色人设驱动的 AI 陪伴

- 为 Live2D 陪伴引入角色人设：内置 MyGO 五人（高松灯、千早爱音、要乐奈、长崎爽世、椎名立希）背景，其余角色走通用兜底。
- 学习鼓励与触摸反馈调用 AI 时，会把当前角色的人设注入提示词，让不同模型的回复具备不同语气和风格。
- 设置页新增“角色人设”编辑区，支持按角色修改名字、背景、语气、禁忌与示例，并保存到本地；未配置依赖的人设字段自动回退到内置默认。
- 修复人设编辑区因 `escape`/`escapeAttr` 作用域导致的初始化失败。

### 目录检索与修正

- 修复按角色名（尤其英文名，如 Anon / Tomori）无法检索到 Live2D 模型的问题：英文查询现在同时匹配模型别名与拆分词。
- 目录检索支持按角色编号（如 037）及角色名别名查找，并对全角/空格做归一化。
- 新增英文名与角色编号的目录检索回归测试。

## 0.73 — Live2D 陪伴触摸反馈与全局引用修复

### 双栏陪伴学习

- 顶部新增“陪伴学习”入口：左侧保持墨墨背词页，右侧显示 Live2D Anon、当前单词、本轮正确率与专注时长；退出后恢复进入前的仪表盘布局。
- 修复“陪伴学习”入口只显示、未绑定行为的问题：公开模块现在挂载到 `window`，点击后会立即切换到双栏陪伴页。
- 修复桌面窗口进入全屏时 Live2D 画布被连续 resize 销毁的问题：改为尺寸重算、加载互斥、异步代次校验和 WebGL 上下文恢复，模型加载期间保留 GIF 兜底。
- Live2D 新增触摸系统：点到头部会微笑鼓励、点到胸/腹部会害羞、点到下体位置会生气；两侧手部还可击掌，反馈气泡与动作随部位切换，并把触摸部位与情绪交给已配置的 AI 生成回应，未配置或离线时回退到本地文案。
- 修复前端 `const` 模块被误当作 `window` 属性判断的问题：学习会话/触摸反馈的 AI 回复、退出陪伴后的布局恢复、图表重绘与学习同步状态刷新现在都能正常触发。
- 学习会话只在真正的背词页记录；四个判断按钮、14 项快捷键、TTS 返回和“加入复习”弹窗避让逻辑保持原样。
- 在第 5 题、每 10 题、连续三次需要巩固、连续学习 12 分钟、结束一轮及手动点击时生成反馈；每次反馈有 90 秒冷却，不会阻塞背词。
- AI 使用现有 API 配置，仅读取本轮汇总、当前单词和最近判断；未配置或离线时自动使用本地鼓励文案与动作状态。

### Live2D 模型衣柜

- 设置页新增 Bestdori 角色/服装目录检索、下载进度、取消、当前模型切换、删除和本地模型文件夹导入。
- 支持 Cubism 2 的 `.moc/.model.json/.mtn` 与 Cubism 3/4 的 `.moc3/.model3.json/.motion3.json`；安装前校验描述文件、相对路径、主文件、贴图及大小。
- 模型保存在 `data/live2d/models/`，统一 EXE 与 GitHub Release 不内置角色模型；受控资源接口不会暴露其他 `data/` 文件。
- WebGL 或模型不可用时自动回退到现有 Anon GIF，保证背词功能持续可用。
- 清理未使用的 Python 导入与本地临时/备份产物，保持运行与发布链路整洁。

### 9 月 6 日

- 当天使用爱音（角色 037）模型进入陪伴学习时，显示一次生日开场祝福与庆祝状态；其他日期不影响既有页面。

## 0.71 — Anon的笔记本与单词详情弹窗避让

### Anon的笔记本命名统一

- 所有有效页面、设置选项、磁贴名称、说明文档和相关注释统一使用“Anon的笔记本”。
- 保留 `notebook` 存储值、CSS 选择器、资源路径和 iframe 通讯结构，原有风格偏好与功能兼容。

### 加入复习弹窗避让

- 点击单词后出现“加入复习 / 加入背诵 / 加入学习”弹窗时，底部四个判断按钮会自动平滑滑出可视区域。
- 按钮在弹窗期间变为不可点击、不可聚焦，数字快捷键也暂停，避免误触影响加入复习操作。
- 弹窗关闭后，四个按钮和快捷键立即恢复；普通版与 Anon的笔记本版均适配。

## 0.70 — 数据中心与运行状态大幅升级

### 数据中心重构

- 运行期主库改为内置 SQLite，数据库文件位于 `data/memo-superform.db`，启用 WAL、外键约束、忙等待和原子写入。
- 学习记录以 `voc_id` 作为稳定主键，重复拼写不会互相覆盖。
- 每日统计和历史快照保持冻结；普通更新不会重写 2020–2022 等历史数据。
- 旧 SQL Server 保留为只读迁移和核验来源，不再要求 SQL Server 才能启动应用，也不会向旧库写入数据。
- 推荐引擎迁移到 SQLite，重新计算时保留已复习状态。

### 按需增量更新

- 启动优先读取本地已提交数据，后台再检查更新，避免页面长时间空白。
- 通过墨墨 `get_today_items` 指纹识别今日变化，只按变化的 `voc_id` 定向拉取完整记录。
- 增加到期候选和未来 30 天活动窗口；远端数量不变时不扫描活动窗口。
- 删除旧的 2020–2200 全量扫描、无过滤补拉和不受官方支持的 `offset` 分页。
- 未变化记录不执行业务字段更新，数据无变化时不重绘图表。
- 支持首次可信浏览器缓存基线、首次必要的完整建库、每周完整核验、设置内手动完整核验、取消和失败续传。
- 对 1000 条上限、空日期、数量不一致、429、网络错误和部分响应采用失败闭合处理，不误删本地数据。
- 增加 10 秒、60 秒和 5 小时多窗口限流、`Retry-After` 处理、指数退避和取消响应。

### 页面与兼容性

- 顶部刷新、自动刷新和 AI 学习记录入口统一使用增量同步。
- 设置面板新增数据更新状态、同步时间、覆盖范围、完整核验和取消更新入口。
- 同步期间保留旧图表；只有提交后的数据确实变化时才统一重绘。
- 保留 `MaimemoAPI.getAllStudyRecords()`、`/api/snapshot`、推荐接口及现有图表数据结构。
- 普通版暗色、Anon的笔记本版浅色、字体、GIF、背词快捷键、背词页面和视觉交互保持不变。

### Windows 后台运行状态

- Windows 右下角通知区域新增 Memo Superform 原生托盘图标。
- 图标提示当前网页/桌面模式和运行状态；双击或“打开 Memo Superform”可恢复页面或窗口。
- 桌面窗口关闭后隐藏到托盘；“退出 Memo Superform”才会停止后台服务并移除图标。
- 再次启动同一 EXE 会通过本地单实例通道唤醒已有实例，不再弹出含糊的“已在运行中”错误。
- Windows EXE 使用无控制台模式，启动与服务日志写入 `data/launcher.log` 和 `data/server.log`。
- 托盘实现只使用 Windows 自带 Shell API，不新增运行时第三方依赖；非 Windows 平台自动降级为无操作。

### 打包与验证

- 网页模式和桌面模式继续共用同一个统一 EXE。
- PyInstaller 资源清单加入 SQLite 架构和托盘模块，保留原字体与 GIF 打包范围。
- 源码回归套件扩展至 35 项，覆盖 SQLite、增量同步、限流、取消、托盘和单实例唤醒。
- 本地候选包：`MemoSuperform-v0.70.exe`。
