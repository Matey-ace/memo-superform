# 停用内容归档

此目录保留已经停用但未来可能恢复的源码，不参与统一 EXE 打包：

- `easter-egg.js`：旧手账彩蛋脚本；引用的 GIF 仍原样保留在 `img/gifs/`。
- `easter-egg.css`：从正式主题移出的停用样式。
- `MemoSuperform-Desktop.spec` / `MemoSuperform-Web.spec`：统一入口之前的旧打包配置。

正式 Windows 发布仅使用项目根目录的 `MemoSuperform.spec`。
