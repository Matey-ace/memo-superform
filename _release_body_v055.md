# 🎉 v0.55 大更新

## ⭐ 重磅新功能

- **单 exe 双模式启动**：桌面模式（pywebview）与网页模式合并进同一个 exe，启动时自由选择，可记住偏好
- **语音合成资源包（GPT-SoVITS）**：全新支持语音朗读！AI 单词分类、智能复习推荐等结果可朗读，内置祥子音色，模型离线随包分发
- **GPU 加速**：自动检测 NVIDIA 显卡并启用 CUDA，热态合成约 0.6 秒/句（12GB 显存即可流畅运行）
- **背单词快捷键**：空格键一键聚焦拼写输入框，无需再点鼠标
- **应用内切换运行模式**：设置面板可直接切换默认模式并一键重启

## 🔧 修复与优化

- anon 笔记本页面白屏问题修复（补齐 tts.js 与设置面板 UI）
- “立即重启”变“直接关闭”的单实例锁时序冲突修复
- WebSocket 上游建连/握手异常保护，失败返回 502 并防止 socket 泄漏
- TTS 状态查询不再阻塞合成过程（busy 走缓存）
- TTS 合成 30 秒超时保护，卡死自动强杀并重置引擎
- install.json 丢失时自动重建，不再误报“未安装”
- tts_state 配置文件损坏容错，状态接口不再 500
- 打包遗漏 schema.sql 导致数据库 503 修复
- 语音功能组件加载问题修复（var TTS 挂载到 window）
- anon 页面 UI 与主界面风格统一
- 资源版本号统一升级，杜绝 WebView 陈旧缓存

## 📦 下载说明

- **MemoSuperform.exe**：主程序（含桌面/网页双模式）
- **tts_pack_v1.0-full.zip.001 / .002**：语音资源包分卷（GitHub 单文件上限 2GB，故拆为两卷）。合并方法：将两卷放同一目录，执行 `copy /b tts_pack_v1.0-full.zip.001+tts_pack_v1.0-full.zip.002 tts_pack_v1.0-full.zip`，得到完整包后按包内 README 三步安装（解压到 data/tts_pack → 运行 setup.bat → 设置里开启语音）
