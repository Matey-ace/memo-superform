# Memo Superform - Linux 依赖
# Web 模式（server.py + 浏览器）仅需 Python 3.9+，无需任何第三方包。
# 以下为桌面模式与可选功能：

# 模式选择窗口需要 python3-tk（系统包，pip 无法安装）：
#   Debian/Ubuntu: sudo apt install python3-tk
# 未安装时仍可用 ./launcher-linux.sh web|desktop 直接指定模式，只是无参数启动没有选择窗口。

# 桌面窗口（pywebview + Qt 后端，pip 可直接安装）
pywebview[qt]>=5.0

# 数据库（可选）：智能复习推荐需要 SQL Server（需另装 Microsoft ODBC Driver 18 for SQL Server）
pyodbc>=4.0

# 打包 Linux 可执行文件（可选）
pyinstaller>=6.0
